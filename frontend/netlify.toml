[build]
  command = "npm install && npm run build"
  publish = "dist"

# SPA redirect: send all non-file routes to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
